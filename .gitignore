node_modules/
package-lock.json
.DS_Store

# Per-site audit results are product evidence, not a publication. They name
# third-party sites and carry heuristic findings, so they stay local.
audits/
reports/audit-*.json
